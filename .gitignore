.fbconfig

# The standalone shop is authored in standalone/ and built into new/.
# Build artefacts and dependencies never get committed; new/ deliberately does,
# because this project has no build step - Vercel publishes the repo as-is.
standalone/node_modules/
standalone/dist/
standalone/.astro/
standalone/.vercel/
