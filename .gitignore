.fbconfig

# Build output. tools/assemble.mjs writes out/ from standalone/dist + old/,
# and Vercel serves that - none of it is committed.
out/
standalone/node_modules/
standalone/dist/
standalone/.astro/
standalone/.vercel/
