.fbconfig

# The shop's built output IS committed at the repo root - Vercel publishes the repo
# as-is. tools/publish.mjs writes it and records what it wrote in .published.
standalone/node_modules/
standalone/dist/
standalone/.astro/
standalone/.vercel/
